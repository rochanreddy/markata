// Component tagger loader for visual editing
module.exports = function(source) {
  // Return the source unchanged for now
  // This loader can be enhanced later for visual editing features
  return source;
};
